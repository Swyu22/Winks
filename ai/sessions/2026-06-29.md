# 2026-06-29 — 接入 Supabase CLI（迁移管理）

**模型**：Claude Opus 4.8
**分支**：`main`

## 1. 安装

`brew install supabase/tap/supabase`（CLI 2.108.0，macOS arm64；官方不支持 `npm -g`）。

## 2. 登录 + 关联（用户执行）

用户跑了 `supabase login` + `supabase link --project-ref ukwuafaoifwdkpvedvih`；`supabase projects list` 确认 Winks 项目 `linked: true`。

## 3. 接入迁移管理（本会话）

- `supabase init` → `supabase/config.toml` + `supabase/.gitignore`（忽略 `.temp`/`.branches`）。
- **目录正规化**：大写 `Supabase/` → 小写 `supabase/`（CLI 固定用小写；macOS 大小写不敏感但 Linux/CI 会当成两个目录）。git 以 rename 跟踪 `Supabase/schema.sql -> supabase/schema.sql`。
- **baseline 迁移**：`supabase migration new initial_schema` → `supabase/migrations/20260628170214_initial_schema.sql`，内容 = 当前 schema（与 `supabase/schema.sql` 同源，完全幂等）。
- 同步更新 8 处文档对 `Supabase/` 路径的引用（README/AGENTS/CLAUDE/.cloud/PROJECT_MAP/adr-0001/0002/0004），README 新增「Supabase CLI（schema 迁移）」一节。

## 4. 交接 / 注意

- 应用到远端：`supabase db push`（需数据库密码）。baseline 幂等，对已建好的远端是安全空操作（仅登记迁移历史）。
- 今后 schema 改动：`supabase migration new <name>` → 写 SQL → `db push`，并同步更新 `schema.sql` 快照（二者同源，注意别让其漂移）。
- 纯数据库工具改动，前端无变化，**未触发 gh-pages 部署**。
- `supabase/.temp/` 已 gitignore（含链接态，勿提交）。
