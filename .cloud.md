# .cloud.md — 当前状态快照

> 这是「现在的项目长什么样」的一页摘要。每次会话结束前必须更新。
> 历史记录请去 `ai/sessions/`，决策请去 `docs/30-decisions/`。

**最后更新**：2026-06-05
**最后更新者**：Codex（Skills 综合审计 + 线上 API / GitHub 状态核验）
**当前分支**：`main`
**最近版本**：`APP_VERSION = v1.2.0`（[src/lib/constants.js](src/lib/constants.js)）

---

## 1. 活跃模块

| 模块 | 文件 | 行数 | 状态 |
|---|---|---:|---|
| 页面编排 | [src/App.jsx](src/App.jsx) | 181 | ✅ 已收敛为编排层，移动分类条在 main 内容流内 |
| 链接状态/CRUD | [src/hooks/useLinks.js](src/hooks/useLinks.js) | 467 | ✅ P1 完成：action helpers 已抽取 |
| UI 组件 | [src/components/](src/components) | 12–334 | ✅ React Doctor 98/100；`LinkModal` 仍偏大但在红线内 |
| Meta 纯函数 | [src/lib/linkMeta.js](src/lib/linkMeta.js) | 117 | ✅ 有 Node 内置测试覆盖 |
| Action 纯函数 | [src/lib/linkActions.js](src/lib/linkActions.js) | 79 | ✅ P1 新增，有 Node 内置测试覆盖 |
| 常量 | [src/lib/constants.js](src/lib/constants.js) | 15 | ✅ 集中管理 |
| Supabase 客户端 | [src/lib/supabaseClient.js](src/lib/supabaseClient.js) | 36 | ✅ 健康 |
| DB Schema | [Supabase/schema.sql](Supabase/schema.sql) | 64 | ✅ 与前端 meta 编码对齐 |

## 2. 最近 5 次提交（main）

```
831679b style: enlarge copy-link button by 30%
86d0938 feat: add board tabs with isolated links and copy button
9dbe052 fix: preserve legacy classifications and improve form accessibility
ce1fd41 perf: apply react best-practice refactor
4fe0d4a fix: align supabase category constraint with metadata format
```

主线方向：**看板（Board）维度稳定化 + 单文件拆分**。Board / Classification / Tags 三层模型已落地，App.jsx 拆分已完成。

## 3. 未决项 / 下一步（按优先级）

详见 [docs/20-plan/current-iteration.md](docs/20-plan/current-iteration.md)。摘要：

- ~~**P1**：继续收敛 `useLinks.js` action helpers~~（✅ 已完成 2026-05-09）。
- ✅ **综合审计**：simplify + security-review skill 全量过 + 手动配置/文档审计（2026-05-09 Opus 4.7）。
  - 关键修复：`runBatchUpdate` 改 `Promise.allSettled` + 失败时 `fetchLinks()` 重新同步（修数据正确性 bug）
  - PIN 字面量抽取为 `ADMIN_PIN` 常量（ADR-0002 行为不变）
  - 修复 `resolveSupabaseClient` 双重 alert bug
  - 新增 `ALL_FILTER` / `PIN_ACTIONS` 常量；`withBoardValues` no-op 短路
  - 安全审计：无新发现高危漏洞（schema CHECK + React escaping 已防御 javascript: 注入）
- ✅ **Skills 综合审计复跑**（2026-06-05 Codex）：
  - 恢复本地依赖安装；`npm audit fix` 非破坏性升级 `ws` 到 8.21.0
  - `LinkModal` 移除打开弹窗时的 prop-sync effect，改为挂载初始化 + 提交时安全派生
  - 补齐按钮 `type` 与输入控件可访问名称；修复移动端分类条被 fixed 顶栏遮挡
  - 验证：`npm test` 15/15、`npm run lint`、`npm run build`、React Doctor 98/100、Playwright 桌面/移动 smoke
- **P0**：仓库已于 2026-06-05 改为 private；当前 GitHub 账户方案不支持私有仓库 Pages。`www.winks.ink` 暂时仍可访问，但 GitHub Pages 设置已停用，需升级 GitHub 方案、恢复 public，或迁移到支持私有仓库的部署平台。
- **P2**：评估 PIN 是否改为环境变量。
- **P2**：评估是否引入 Vitest + Testing Library；当前仅用 Node 内置测试覆盖 [src/lib/linkMeta.js](src/lib/linkMeta.js)。
- **P2**：评估 Vite major 升级；`npm audit fix` 已处理非破坏性依赖漏洞，剩余 dev-only Vite/esbuild 漏洞需要破坏性升级确认。
- **P2**：评估是否将自定义 modal 迁移到原生 `<dialog>`，以及 `LinkModal` 是否进一步 reducer 化。

## 4. 关键约束提醒（最容易踩坑的地方）

1. **改 `links.category` 之前先读** [docs/10-spec/data-model.md](docs/10-spec/data-model.md)。该字段同时是分类与 `__WINKS_META__` JSON 容器。
2. **改 `DEFAULT_BOARDS` 之前**：同步评估存量数据迁移、`isKnownBoard` 校验、UI 看板按钮；常量位于 [src/lib/constants.js](src/lib/constants.js)。
3. **改 `PinModal` 之前先读** [adr-0002](docs/30-decisions/adr-0002-frontend-pin-only-auth.md)。
4. **任何读路径**新增数据返回都必须经 [hydrateLink](src/lib/linkMeta.js)。
5. **UI 组件不直接调用 Supabase**；数据源编排收敛在 [src/hooks/useLinks.js](src/hooks/useLinks.js)。

## 5. 已知 Tech Debt

| 项 | 影响 | 计划 |
|---|---|---|
| ~~`useLinks.js` 内多个回调超过 30 行~~ | ~~阅读成本、后续测试难度~~ | ✅ 已通过 P1 抽取 linkActions.js 解决 |
| UI 组件缺少 React Testing Library 覆盖 | 重构风险 | P2：评估 Vitest + Testing Library |
| PIN 硬编码 | 弱安全 + 不可配置 | P2 |
| Vite/esbuild dev audit 漏洞 | 本地 dev server 风险 | P2：评估 Vite major 升级 |
| `LinkModal` 仍未用 reducer / 原生 dialog | 可维护性与无障碍改进空间 | P2：独立小迭代处理 |

## 6. 不打算做的事（避免重复讨论）

- ❌ 不引入路由库（react-router）：当前是单页面 + 看板 Tab，足够。
- ❌ 不引入状态管理库（zustand/redux）：状态规模在 `useLinks()` 内可控。
- ❌ 不把 RLS 改严格：协作式弱保护是有意选型，见 ADR-0002。

## 7. 环境与协作

- **Hook**：`core.hooksPath = scripts/git-hooks`（pre-commit 在 staged JS/JSX/CJS/MJS 变更时跑 lint）。
- **测试**：`npm test` 使用 Node 内置 test runner，当前覆盖 `linkMeta` 纯函数。
- **环境模板**：使用 `.env.example` 创建本地 `.env`；不要提交真实 `.env`。
- **Demo 模式**：`VITE_DEMO_MODE=true` 跳过 Supabase。
- **部署**：原流程为 `npm run deploy`（gh-pages）；仓库 private 后 Pages 设置显示需升级方案或恢复 public，当前自定义域名存在下线风险。
