# 复制为 .env 后使用。此模板默认 VITE_DEMO_MODE=true，可零配置直接看演示数据。
# 接入 Supabase：填入下面的 URL / ANON_KEY，并把 VITE_DEMO_MODE 改为 false。
# 注意：不提供 .env 时（仓库默认状态）VITE_DEMO_MODE 未定义 = false = Supabase 模式。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_DEMO_MODE=true

